{% extends "base.html.twig" %}

{% block title %}Inscription au site{% endblock %}

{% block body %}
    <div class="container">
        <h1>Inscription sur Immoloc</h1>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Confirmer l'inscription</button>
        {{ form_end(form) }}
    </div>
{% endblock %}

